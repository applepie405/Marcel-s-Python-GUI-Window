from tkinter import *

# Create a window
root = Tk()
root.title("My GUI App")

# Create a label and add it to the window using pack()
labell = Label(root, text="lable2 with longer sentence!")
label2.pack()

# Run the main window loop
root.mainloop()
